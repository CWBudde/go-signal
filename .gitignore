# Build output
/bin/
/dist/

# Test binary, built with `go test -c`
*.test

# Coverage
*.out
coverage.txt
coverage.html
coverage-results.md

# Go workspace file
go.work
go.work.sum

# Local signal data (never commit keys)
/data/
*.db
*.db-wal
*.db-shm

# IDE
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store

# libsignal_ffi.a built by `just libsignal`
/third_party/lib/

# Static (musl) build: libsignal_ffi.a per arch and the Rust/Go caches of `just build-static`
/third_party/lib-musl/
/third_party/.musl/

# Local MCP test setup (points at ./data/)
/.mcp.json
